export type FontWeight = {
  bold: number;
  medium: number;
  regular: number;
  light: number;
};

export const defaultFontWeight: FontWeight = {
  bold: 700,
  medium: 600,
  regular: 500,
  light: 400,
};
